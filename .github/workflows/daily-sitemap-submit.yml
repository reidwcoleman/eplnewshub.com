name: Daily Sitemap Submission

on:
  schedule:
    # Runs daily at 10pm UTC — after all article-scout runs (8am, 12pm, 4pm, 8pm UTC)
    - cron: '0 22 * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  submit-sitemap:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Write Google service account key
        if: env.GOOGLE_SERVICE_ACCOUNT_JSON != ''
        env:
          GOOGLE_SERVICE_ACCOUNT_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: echo "$GOOGLE_SERVICE_ACCOUNT_JSON" > google-service-account.json

      - name: Submit sitemap
        env:
          INDEXNOW_KEY: ${{ secrets.INDEXNOW_KEY }}
        run: node .github/scripts/submit-sitemap.js
