services:
  - type: web
    name: eplnewshub-comments
    runtime: node
    buildCommand: npm install
    startCommand: node comments-server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001